export enum RecurringTransactionsStatus {
    ACTIVE = 'active',
    PAUSED = 'paused',
    COMPLETED = 'completed',
    CANCELED = 'canceled',
    DELETED = 'deleted'
}

export enum TRANSACTION_TYPE {
    DEBIT = 'debit',
    CREDIT = 'credit'
}
