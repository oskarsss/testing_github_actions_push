import { styled } from '@mui/material';

export const Wrapper = styled('div')(() => ({
    height    : '100%',
    width     : 'fit-content',
    display   : 'flex',
    alignItems: 'center',
    margin    : '0 auto'
}));
