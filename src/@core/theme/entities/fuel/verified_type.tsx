import { fraud_icon, unverified_icon, verified_icon } from '@/@core/theme/entities/fuel/icons';

export const VERIFIED_TYPE_ICONS = {
    not_verified: fraud_icon,
    unverified  : unverified_icon,
    verified    : verified_icon
};
