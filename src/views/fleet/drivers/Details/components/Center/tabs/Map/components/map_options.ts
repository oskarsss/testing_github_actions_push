const sources = {
    driver : 'driver',
    truck  : 'truck',
    trailer: 'trailer'
};

const layers = {
    driver : 'driver_layer',
    truck  : 'truck_layer',
    trailer: 'trailer_layer'
};

const images_names = {
    driver : 'driver-image',
    truck  : 'truck-image',
    trailer: 'trailer-image'
};

const map_options = {
    sources,
    layers,
    images_names
};

export default map_options;
