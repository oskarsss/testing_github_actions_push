import { styled } from '@mui/material/styles';

export const CompanyBlock = styled('div')({
    width         : '100%',
    display       : 'flex',
    alignItems    : 'center',
    justifyContent: 'start',
    gap           : 10,
    svg           : {
        width : 32,
        height: 32
    }
});
