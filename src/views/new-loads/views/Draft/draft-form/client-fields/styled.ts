import { styled } from '@mui/material';

export const IconContainer = styled('div')(() => ({
    display       : 'flex',
    alignItems    : 'center',
    justifyContent: 'center',
    width         : 48,
    height        : 48,
    background    : '#E1EEFF',
    borderRadius  : '100%',
    marginBottom  : 16
}));
