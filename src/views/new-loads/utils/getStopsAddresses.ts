// import DraftsTypes from '@/services/drafts/types';

// export const getStopsAddresses = (stops: DraftsTypes.Stops) =>
//     stops.reduce(
//         (acc, stop) => {
//             acc.first_address = stops[0].locationIdAddress;
//             acc.last_address = stops[stops.length - 1].locationIdAddress;
//             return acc;
//         },
//         { first_address: '', last_address: '' } as
// { first_address: string; last_address: string }
//     );
