import { styled } from '@mui/material/styles';

const Container = styled('div')({
    display       : 'flex',
    height        : '100%',
    alignItems    : 'center',
    justifyContent: 'center',
    marginTop     : '50px',
    marginBottom  : '50px'
});

const AuthLoaderStyled = {
    Container
};

export default AuthLoaderStyled;
